# Local integration test database. Isolated from the interactive dev database
# (.env.local) so running `pnpm test:integration` can never write into dev data.
POSTGRES_TEST_PORT=5434
DATABASE_URL=postgresql://usstm:usstm@127.0.0.1:5434/usstm_portal_test
